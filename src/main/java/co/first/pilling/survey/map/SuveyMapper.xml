<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper
	namespace="co.first.pilling.survey.map.SurveyMapper">

	<!-- 설문조사 결과 삽입 쿼리  -->
	<insert id="surveyResultInput">
	    INSERT INTO survey_responses (user_no, keyword_id)
	    VALUES (#{userNo}, #{keywordId});
	</insert>
</mapper>