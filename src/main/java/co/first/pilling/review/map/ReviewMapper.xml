<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="co.first.pilling.review.map.ReviewMapper">

<select id="reviewSelectList">
SELECT * FROM review
</select>

<select id="reviewSelect">
SELECT * FROM review WHERE user_id=#{userId}
</select>

<update id="reviewUpdate">
UPDATE review
SET rating=#{rating}, review_title=#{reviewTitle}, review_content=#{reviewTitle}, review_date=now(), review_image=#{reviewImg}
WHERE user_id = #{userId}
</update>

<delete id="reviewDelete">
DELETE 
FROM review
WHERE review_id =#{reviewId}
</delete>
</mapper>
